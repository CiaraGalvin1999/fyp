'use strict'
import { StyleSheet } from 'react-native'

module.exports = StyleSheet.create({
    removeFicTitle: {
        color: '#FFFFFF',
        fontWeight: 'bold',
        fontFamily: 'Roboto',
        fontSize: 16,
        textAlign: 'center',
        padding: 16,
        fontStyle: 'italic',
    }
})